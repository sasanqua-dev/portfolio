export interface Event {
  id: string;
  name: string;
  category: string;
  event: string;
  repo: string;
  reward: string;
  slide: string;
  sponsored: string;
  start_date: string;
  end_date: string;
  created: string;
  updated: string;
  tag: string[];
}